trigger:
- master

pool:
  vmImage: 'win1803'

container:
  image: 'fmcclean/anaconda-windows:weather-reporter'
  endpoint: 'Docker Registry'

steps:

- script: python -m unittest tests/test_layout.py
  displayName: 'run test_layout.py'
- pubilsh: 'all_plots.pdf'


